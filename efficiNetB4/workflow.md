# EfficientNet-B4 Multi-Label Classification — Workflow

## 개요

`data2/` 디렉토리의 전입신고서 이미지에 대해 14개 필드 기입 여부를 판별하는 **Multi-Label Classification** 파이프라인.
EfficientNet-B4 (pretrained) 기반으로 Optuna 하이퍼파라미터 최적화와 GradCAM 시각화를 포함한다.

---

## 데이터 구조

| Split | 이미지 경로    | 레이블 파일              | 샘플 수 |
| ----- | -------------- | ------------------------ | ------- |
| Train | `data2/train/` | `data2/train_labels.csv` | 649     |
| Val   | `data2/val/`   | `data2/val_labels.csv`   | 147     |
| Test  | `data2/test/`  | `data2/test_labels.csv`  | 151     |

- **이미지 형식**: JPG, 플랫 디렉토리 구조 (클래스별 서브폴더 없음)
- **CSV 구조**: `filename` 컬럼 + 14개 이진(0/1) 레이블 컬럼
- **레이블 대상 (14개)**: 전입자*성명, 전입자*주민등록번호, 전입자*연락처, 전입자*서명도장, 전*시도, 전*시군구, 현*세대주성명, 현*연락처, 현*서명도장, 현*주소, 전입사유*체크, 우편물서비스*동의체크, 신청인*성명, 신청인*서명도장

---

## 구현 단계 (Phase)

### Phase 1 — 데이터 레이어

**1. Config 상수 정의**

- `NUM_CLASSES = 14`
- `EPOCHS = 20`, `OPTUNA_EPOCHS = 5`, `N_TRIALS = 20`
- `BEST_MODEL_PATH = "best_model_efficiB4.pth"`
- 이미지/CSV 경로: `../data2/`

**2. Transform 정의**

- `transform_train`: 224×224 리사이즈 + `ColorJitter(brightness=0.2, contrast=0.2)` + ImageNet 정규화
- `transform_val/test`: 리사이즈 + ImageNet 정규화만 적용 (증강 없음)

**3. MultiLabelDataset (Custom Dataset)**

- `ImageFolder` 사용 불가 (플랫 디렉토리)
- CSV `filename` 컬럼으로 이미지 로드
- 나머지 14컬럼 → `float32` 텐서로 반환

```python
# =====================================================================
# [주의] ImageFolder를 사용하지 않는 이유
# =====================================================================
# ImageFolder는 아래와 같은 클래스별 서브폴더 구조를 전제로 동작한다:
#
#   train/
#   ├── cat/
#   │   ├── img1.jpg
#   │   └── img2.jpg
#   └── dog/
#       └── img3.jpg
#
# 이 프로젝트의 실제 구조는 플랫(flat) 디렉토리다:
#
#   data2/train/
#   ├── 학습-전입신고서1.jpg
#   ├── 학습-전입신고서2.jpg
#   └── ...  ← 모든 이미지가 하나의 폴더에 평탄하게 존재
#
# 레이블은 이미지 폴더가 아닌 별도 CSV 파일에 존재하며,
# 한 이미지에 14개의 이진 레이블이 동시에 붙는 multi-label 구조다.
# ImageFolder는 "폴더명 = 단일 클래스" 방식이므로 이 구조를 읽을 수 없다.
#
# ※ 학습 성능에는 영향 없음 — 디렉토리 구조는 파일 읽기 방식의 문제일 뿐이며,
#   모델이 학습하는 대상은 (image_tensor, label_tensor) 쌍으로 동일하다.
#
# → torch.utils.data.Dataset을 직접 상속한 커스텀 Dataset을 사용한다.
# =====================================================================
```

---

### Phase 2 — 모델 구성

**4. `build_model()` 함수**

- `efficientnet_b4(pretrained=True)` 로드
- 전체 파라미터 freeze (`requires_grad = False`)
- `model.features[-1]` unfreeze (Fine-tuning 대상)
- `model.classifier[1] = nn.Linear(1792, 14)` 교체

> wd8_fine_final.ipynb의 `layer4` unfreeze + FC 교체 패턴 적용

---

### Phase 3 — Optuna 하이퍼파라미터 최적화

**5. `objective(trial)` 함수 정의**

- 탐색 파라미터:
  - `batch_size`: categorical `[4, 8, 16]`
  - `lr`: loguniform `1e-5 ~ 1e-3`
- 손실함수: `BCEWithLogitsLoss` (Multi-label)
- `OPTUNA_EPOCHS = 5` 루프로 빠른 탐색
- `trial.report(val_loss, epoch)` + `trial.should_prune()` 으로 조기 종료

**6. 최적화 실행**

```python
study = optuna.create_study(direction="minimize")
study.optimize(objective, n_trials=20)
print(study.best_trial.params)
```

> wd_optuna.ipynb의 objective 구조, pruning 패턴 그대로 적용

---

### Phase 4 — 최종 학습 & 평가

**7. 최적 파라미터로 재학습**

- Optuna에서 찾은 `batch_size`, `lr`로 DataLoader 재생성
- `tqdm` 진행 표시
- Early Stopping (patience = 3): val_loss 개선 시 `best_model_efficiB4.pth` 저장

```python
# =====================================================================
# [Early Stopping] patience = 3 의 동작 방식
# =====================================================================
# val_loss가 연속 3번 개선되지 않으면 학습을 조기 종료한다.
# 개선이 있을 때마다 best_model_efficiB4.pth 를 덮어저장하며,
# 종료 시점이 아닌 "가장 좋았던 시점"의 모델이 최종 결과물이 된다.
#
# 예시 (EPOCHS=20 기준):
#   Epoch 1 : val_loss=0.45 → 갱신 ✅  (저장, count=0)
#   Epoch 2 : val_loss=0.42 → 갱신 ✅  (저장, count=0)
#   Epoch 3 : val_loss=0.44 → 개선 없음 (count=1)
#   Epoch 4 : val_loss=0.46 → 개선 없음 (count=2)
#   Epoch 5 : val_loss=0.45 → 개선 없음 (count=3) → 학습 중단!
#   → 최종 모델 = Epoch 2 시점의 가중치
#
# patience 값 선택 근거:
#   patience=1 : 너무 민감 — val_loss 소폭 상승 시 즉시 종료, 지역 최솟값 위험
#   patience=3 : 균형 ✅  — 소규모 데이터(649장)+EPOCHS=20 조합에 적합
#                           자연적 loss 진동(노이즈) 1~2 epoch 허용 후 종료
#   patience=5+ : 느슨   — 과적합 구간을 오래 허용, best_model과 gap 커짐
# =====================================================================
```

**8. Test 평가**

- `sigmoid(output) > 0.5` → 이진 예측
- Element-wise Accuracy 출력

> wd8_fine_final.ipynb의 early stopping + test 평가 구조 적용

---

### Phase 5 — GradCAM 시각화

**9. GradCAM + GradCAMPlusPlus**

- `best_model_efficiB4.pth` 로드
- test셋 첫 번째 이미지 자동 선택
- `target_layers = [model.features[-1]]`
- `ClassifierOutputTarget(pred_class)` — 가장 높은 confidence 클래스 기준
- subplot 2개로 GradCAM / GradCAMPlusPlus 비교 시각화
- 결과 `gradcam_result.png` 저장

> wd8_fine_final.ipynb의 GradCAM 코드 구조 적용, `model.layer4` → `model.features[-1]` 변경

---

## 참조 파일

| 파일                        | 참조 내용                                                              |
| --------------------------- | ---------------------------------------------------------------------- |
| `wd8_fine_final.ipynb`      | transform 정의, train loop, early stopping, GradCAM 코드 구조          |
| `wd_optuna.ipynb`           | objective 함수, batch_size/lr 탐색, pruning, `study.best_trial.params` |
| `data2/train_labels.csv` 등 | 14개 이진 레이블, 파일명 참조                                          |

---

## 결정 사항 요약

| 항목                    | 결정값                       | 근거                    |
| ----------------------- | ---------------------------- | ----------------------- |
| 입력 해상도             | 224×224                      | 속도/메모리 우선        |
| Optuna n_trials         | 20                           | 탐색 효율과 시간의 균형 |
| Optuna epochs           | 5                            | 탐색 시 빠른 수렴 판단  |
| 손실함수                | BCEWithLogitsLoss            | Multi-label 이진 분류   |
| Early Stopping patience | 3                            | 과적합 방지             |
| GradCAM 대상            | test셋 첫 번째 이미지 (자동) | 재현성 보장             |

---

## 데이터 증강 선택 근거 — 3가지 비교

전입신고서는 **정형화된 행정 양식 스캔 이미지**라는 도메인 특성상, 일반 자연 이미지에 사용하는 증강 기법을 그대로 적용하면 오히려 역효과가 발생할 수 있다.

| 항목                | `RandomHorizontalFlip`                               | `RandomRotation(±3°)`                     | `ColorJitter(brightness, contrast)` ✅ |
| ------------------- | ---------------------------------------------------- | ----------------------------------------- | -------------------------------------- |
| 대응 문제           | 좌우 반전                                            | 스캔 각도 편차                            | 스캐너/복사기 밝기·대비 편차           |
| 이 데이터 발생 확률 | ❌ 없음                                              | ⚠️ 낮음                                   | ✅ 높음                                |
| 레이블 영향         | ❌ 한글 텍스트·필드 레이아웃 거울상 반전 → 의미 훼손 | ⚠️ 미세 기울기로 필드 경계 위치 이동 가능 | ✅ 없음 (내용 자체 불변)               |
| 서명/도장 영향      | ❌ 거울상으로 왜곡                                   | ⚠️ 미세 회전 허용 가능                    | ✅ 농도 변화만, 형태 보존              |
| 모델 학습 효과      | ❌ 반전 양식을 정상으로 학습하여 혼란 유발           | ⚠️ 미세 각도 보정에 소폭 기여             | ✅ 잉크 농도·스캔 품질 차이에 강건     |
| **적합성**          | **❌ 부적합**                                        | **⚠️ 선택적 사용**                        | **✅ 채택**                            |

**`RandomHorizontalFlip` 제외 이유**: 양식지를 좌우 반전하면 한글 텍스트와 필드 레이아웃이 거울상이 되어 모델이 비정상 입력을 정상 데이터로 학습하게 된다. 서명·도장 이미지도 반전되어 특징이 왜곡된다.

**`RandomRotation` 미채택 이유**: 실제 스캔 시 각도 편차가 발생하므로 이론적 근거는 있으나, 회전 시 양식 테두리와 필드 선이 잘리거나 보간(interpolation)으로 픽셀 값이 변경될 위험이 있다. 소규모 데이터(649장)에서 잘못된 기하 변환은 과적합을 유발할 수 있어 채택하지 않았다.

**`ColorJitter` 채택 이유**: 전입신고서의 핵심 판별 기준은 필드에 **잉크(텍스트/서명/도장)가 있는가** — 즉 잉크와 종이 배경의 명암 대비다. 스캐너·복사기 기종에 따른 밝기·대비 편차는 현실에서 자주 발생하며, `ColorJitter`는 이미지 내용(텍스트·서명 위치)을 변형하지 않고 광도 특성만 변화시켜 모델의 조건 불변성(condition invariance)을 높인다. `saturation`·`hue`는 흑백에 가까운 문서 이미지에서 효과가 없으므로 제외한다.

```python
# 채택
transforms.ColorJitter(brightness=0.2, contrast=0.2)

# 제외
# transforms.RandomHorizontalFlip(p=0.5)  # 한글 양식 거울상 반전 → 레이아웃 의미 훼손
# transforms.RandomRotation(degrees=3)    # 필드 경계 이동 및 보간 왜곡 위험
```

---

## 검증 체크리스트

- [ ] `python efficiNetB4_main.py` 실행 시 Optuna trial 로그 출력
- [ ] best params 출력 후 재학습 tqdm 진행 확인
- [ ] Test Accuracy 수치 출력 확인
- [ ] `gradcam_result.png` 생성 확인

---

## EfficientNet-B4 모델 선택 근거 — 타 모델 대비 비교 분석

### 데이터 특성 요약

본 프로젝트의 데이터는 다음 특성을 가진다:

- **도메인**: 한국 행정 양식 (전입신고서) 스캔 이미지
- **Task**: Multi-label classification (14개 이진 레이블)
- **총 학습 데이터**: 약 649장 (소규모)
- **클래스 불균형**: 일부 필드는 ~95% 존재, 일부는 ~50% 미기입
- **이미지 특성**: 고밀도 텍스트, 정형화된 양식 구조, 필드 위치 고정

---

### 모델별 상세 비교

#### 1. VGG16 (기존 사용 모델)

| 항목                      | 내용                                                                                                                                                                                                   |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 출시 연도                 | 2014                                                                                                                                                                                                   |
| 파라미터 수               | 약 138M                                                                                                                                                                                                |
| Top-1 Accuracy (ImageNet) | 71.6%                                                                                                                                                                                                  |
| 소규모 데이터 적합성      | ❌ 낮음                                                                                                                                                                                                |
| **문제점**                | 파라미터 과다로 소규모 데이터에서 과적합 위험이 매우 높음. 고정 224×224 입력으로 문서 디테일 손실. 단순 Linear 스택 구조로 멀티스케일 특징 추출 불가. 학습/추론 속도 느림 (FC 레이어만 147M 파라미터). |

**결론**: 현재 코드베이스에서 사용 중이나 이 데이터셋엔 부적합.

---

#### 2. ResNet-34 / ResNet-50 (참조 노트북 사용 모델)

| 항목                 | ResNet-34                                                                                                                                                                                     | ResNet-50 |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 파라미터 수          | 약 21M                                                                                                                                                                                        | 약 25M    |
| Top-1 Accuracy       | 73.3%                                                                                                                                                                                         | 76.1%     |
| 소규모 데이터 적합성 | ✅ 양호                                                                                                                                                                                       | ✅ 양호   |
| 학습 속도            | 빠름                                                                                                                                                                                          | 보통      |
| **문제점**           | Skip connection으로 VGG 대비 개선됐으나, 단일 스케일 특징 추출 구조로 문서 양식의 다양한 크기 요소(필드 레이블, 서명란, 체크박스)를 동시에 효율적으로 처리하기 어려움. Compound Scaling 없음. |

**결론**: 베이스라인으로는 적합하나, EfficientNet 대비 정확도와 파라미터 효율 모두 열세.

---

#### 3. MobileNetV3 (경량 모델)

| 항목                 | 내용                                                                                                                                                                          |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 파라미터 수          | 약 5.4M                                                                                                                                                                       |
| Top-1 Accuracy       | 75.2%                                                                                                                                                                         |
| 소규모 데이터 적합성 | ✅ 높음                                                                                                                                                                       |
| 학습 속도            | 매우 빠름                                                                                                                                                                     |
| **문제점**           | 모바일 환경 최적화 목적으로 설계되어 정확도가 EfficientNet 대비 낮음. Depthwise Separable Conv의 표현력 한계로 문서의 미세한 필드 구분(서명/도장 유무) 판별 정확도 저하 우려. |

**결론**: 속도 최우선 시 고려 가능하나, 정확도 희생이 불가피.

---

#### 4. Vision Transformer (ViT-B/16)

| 항목                 | 내용                                                                                                                                                                                                                                                                              |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 파라미터 수          | 약 86M                                                                                                                                                                                                                                                                            |
| Top-1 Accuracy       | 81.8%                                                                                                                                                                                                                                                                             |
| 소규모 데이터 적합성 | ❌ 낮음                                                                                                                                                                                                                                                                           |
| 학습 속도            | 느림                                                                                                                                                                                                                                                                              |
| **문제점**           | Transformer 계열 모델은 대규모 데이터(ImageNet-21k 이상)에서 사전학습 시 뛰어나지만, **소규모 fine-tuning 데이터(649장)에서는 일반화 성능이 크게 저하**됨. 전역 Self-Attention 연산으로 GPU 메모리 요구량이 높음. 16×16 패치 분할 시 문서의 작은 필드(체크박스) 정보 손실 가능성. |

**결론**: 데이터가 수천 장 이상으로 늘면 재고려 가능하나, 현재 규모에서는 과도한 모델 복잡도.

---

#### 5. LayoutLM / LayoutLMv3 (문서 특화 모델)

| 항목                 | 내용                                                                                                                                                                                          |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 파라미터 수          | 약 113M~                                                                                                                                                                                      |
| 문서 이해 성능       | 최고 수준                                                                                                                                                                                     |
| 소규모 데이터 적합성 | ⚠️ 조건부                                                                                                                                                                                     |
| **문제점**           | 이미지와 텍스트(OCR)를 함께 처리하는 멀티모달 모델로, **OCR 전처리 파이프라인이 별도로 필요**함. 한국어 양식에 대한 사전학습 데이터가 제한적. 구현 복잡도가 높아 프로젝트 범위를 크게 벗어남. |

**결론**: 최고 성능이나 OCR 파이프라인 구축 비용이 크므로 현 프로젝트 범위 밖.

---

#### 6. EfficientNet-B4 ✅ (선택)

| 항목                      | 내용                           |
| ------------------------- | ------------------------------ |
| 파라미터 수               | 약 19M                         |
| Top-1 Accuracy (ImageNet) | 83.0%                          |
| 소규모 데이터 적합성      | ✅ 높음                        |
| 학습 속도                 | 빠름                           |
| 입력 해상도 (기본)        | 380×380 (본 프로젝트: 224×224) |

**EfficientNet-B4를 선택한 이유:**

1. **Compound Scaling**: 네트워크의 깊이(depth), 너비(width), 해상도(resolution)를 동시에 균형 있게 확장하는 EfficientNet 고유의 설계 방식. 동일 파라미터 수 대비 타 모델보다 높은 정확도를 달성.

2. **소규모 데이터 과적합 저항성**: ResNet-50(25M)과 유사한 파라미터 수(19M)이면서 VGG16(138M) 대비 압도적으로 적은 파라미터로, 649장의 학습 데이터에서 과적합 위험이 현저히 낮음.

3. **멀티스케일 특징 추출**: MBConv(Mobile Inverted Bottleneck Conv) 블록이 다양한 해상도에서 특징을 추출하여, 크기가 다른 양식 필드(서명란, 체크박스, 텍스트 필드)를 동시에 효과적으로 포착.

4. **Fine-tuning 친화성**: `features[-1]`만 unfreeze하는 부분 fine-tuning으로도 빠르게 도메인 적응이 가능. ImageNet pretrained 가중치의 일반적 특징(엣지, 텍스처)이 문서 이미지에도 유효하게 전이됨.

5. **GradCAM 호환성**: `model.features[-1]` 레이어가 CNN 계열 마지막 Conv 블록으로, GradCAM 시각화 시 공간적 활성화 맵 생성이 명확함. ViT와 달리 spatial feature map 구조가 유지됨.

6. **B-시리즈 내 위치**: EfficientNet은 B0~B7까지 존재. B0~B2는 정확도 부족, B5 이상은 소규모 데이터에서 오히려 과적합 위험 증가. **B4가 정확도와 과적합 방지의 최적 균형점**.

---

### 최종 모델 비교 요약표

| 모델                | 파라미터 수 | ImageNet Top-1 | 소규모 데이터 | 학습 속도 | 문서 특징 추출 | GradCAM 적합성 | 종합    |
| ------------------- | ----------- | -------------- | ------------- | --------- | -------------- | -------------- | ------- |
| VGG16               | 138M        | 71.6%          | ❌            | ❌        | ❌             | ✅             | ❌      |
| ResNet-34           | 21M         | 73.3%          | ✅            | ✅        | ✅             | ✅             | ✅      |
| ResNet-50           | 25M         | 76.1%          | ✅            | ✅        | ✅             | ✅             | ✅      |
| MobileNetV3         | 5.4M        | 75.2%          | ✅            | ★★★       | ⚠️             | ✅             | ⚠️      |
| ViT-B/16            | 86M         | 81.8%          | ❌            | ❌        | ✅✅           | ⚠️             | ⚠️      |
| LayoutLMv3          | 113M+       | N/A            | ⚠️            | ❌        | ★★★            | ❌             | ⚠️      |
| **EfficientNet-B4** | **19M**     | **83.0%**      | **✅✅**      | **✅**    | **✅✅**       | **✅**         | **★★★** |

> **결론**: EfficientNet-B4는 소규모 문서 이미지 multi-label 분류에서 정확도, 파라미터 효율, fine-tuning 용이성, GradCAM 호환성의 모든 기준을 충족하는 최적 모델이다.
